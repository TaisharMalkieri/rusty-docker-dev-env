version: '3.8'

volumes:
  db-data:

services:
  backend:
    build:
      context: backend
      dockerfile: Dockerfile
      target: Dev
    env_file:
      - ./.devenv/.cargo.env

  db:
    image: redis
    restart: always
    healthcheck:
      test: [ "CMD", "redis-server", "ping", "-h", "127.0.0.1", "--silent" ]
      interval: 3s
      retries: 5
      start_period: 30s
    volumes:
      - db-data:/var/lib/redis
    expose:
      - 6379

  proxy:
    build:
      context: proxy
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - backend